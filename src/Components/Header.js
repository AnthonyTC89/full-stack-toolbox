import React from 'react';
import './Header.css';

const Header = () => (
  <header className="container my-2">
    <h1 className="header-title text-primary">
      Choice Técnico - Toolbox
    </h1>
  </header>
);

export default Header;
